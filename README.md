# Stellar Path - 星途

基于 ECharts 树图的计算机知识技能树可视化系统。

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## 核心特性

### 视觉设计
- 赛博朋克风格界面
- 三种节点状态：未解锁、可学习、已掌握
- 树形结构清晰展示技能依赖关系
- 粒子背景特效

### 功能
- 点击节点切换状态
- 智能前置条件检查
- 实时进度统计
- 节点详情面板
- 缩放和拖拽支持

### 数据管理
- localStorage 自动保存
- JSON 格式导出/导入
- 进度重置

### 知识体系
包含 50+ 个计算机知识点，涵盖：
1. 计算机基础 - 数据结构、算法、操作系统、网络
2. 编程语言 - Python、JavaScript、C++、TypeScript
3. 人工智能 - 机器学习、深度学习、OpenCV、NLP
4. Web 开发 - React、Vue、Node.js、Express
5. 数据库 - SQL、MySQL、Redis、MongoDB
6. 网络安全 - Web安全、SQL注入、密码学、逆向工程
7. 运维 DevOps - Linux、Docker、Kubernetes、CI/CD

## 快速开始

### 启动项目

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 浏览器打开 http://localhost:3000
```

### 环境要求
- Node.js >= 16.x (推荐 18.x)
- npm >= 8.x

### 安装 Node.js

使用 nvm:

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 加载配置
source ~/.bashrc

# 安装 Node.js 18
nvm install 18
nvm use 18
```

### 构建生产版本

```bash
npm run build
npm run preview
```

## 📖 使用指南

### 基本操作

1. **解锁和点亮技能**
   - � 点击未解锁的节点
     - 如果前置技能已完成 → 解锁为可学习状态
     - 否则提示需要完成前置技能
   - 🔓 点击可学习的节点 → 变为已掌握状态 ✨
   - ✨ 点击已掌握的节点 → 退回到可学习状态（可选）

2. **查看节点详情**
   - 悬停在节点上：查看 Tooltip 简介
   - 点击节点：在左下角显示详细信息面板

3. **缩放和导航**
   - 鼠标滚轮：缩放视图
   - 鼠标拖拽：平移画布
   - 节点拖拽：调整节点位置

### 数据管理

#### 自动保存
- 每次点击节点后，进度自动保存到浏览器 `localStorage`
- 下次打开自动恢复上次的学习进度

#### 手动导出
```
点击右下角 "💾 导出进度" 按钮
→ 自动下载 skill_tree_progress_YYYY-MM-DD.json
```

#### 导入进度
```
点击 "📂 导入进度" 按钮
→ 选择之前导出的 JSON 文件
→ 加载成功后自动应用并保存
```

#### 重置进度
```
点击 "🔄 重置进度" 按钮
→ 确认后清空所有进度
→ 重新开始修行之旅
```

## 🛠️ 技术栈

- **前端框架**: React 18
- **构建工具**: Vite 5
- **可视化库**: ECharts 5 (树图)
- **图表组件**: echarts-for-react
- **样式方案**: Tailwind CSS 3

## 📂 项目结构

```
skill tree/
├── src/
│   ├── components/
│   │   ├── SkillTree.jsx          # 核心技能树组件
│   │   └── ParticleBackground.jsx # 粒子背景特效
│   ├── data/
│   │   └── skillTreeData.json     # 知识体系数据 (50+ 节点)
│   ├── App.jsx                    # 主应用组件
│   ├── App.css                    # 全局样式和动画
│   ├── main.jsx                   # React 入口
│   └── index.css                  # 基础样式
├── public/                        # 静态资源
├── index.html                     # HTML 模板
├── package.json                   # 项目配置
├── vite.config.js                 # Vite 配置
├── tailwind.config.js             # Tailwind 配置
├── README.md                      # 本文档
└── ADVANCED_FEATURES.md           # 功能扩展建议
```

## 🎨 自定义知识体系

你可以通过编辑 `src/data/skillTreeData.json` 来添加自己的知识点：

### 数据结构说明

```json
{
  "meta": {
    "version": "1.0.0",
    "theme": "cyber_cultivation"
  },
  "categories": [
    {
      "id": 0,
      "name": "分类名称",
      "color": "#00f2ff"  // 节点发光颜色
    }
  ],
  "nodes": [
    {
      "id": "unique_id",          // 唯一标识
      "name": "显示名称",
      "category": 0,              // 所属分类 ID
      "status": "locked",         // locked | unlocked | mastered
      "desc": "技能描述",
      "prerequisites": ["node1"]  // 前置技能 ID 数组
    }
  ]
}
```

### 添加新技能示例

```json
{
  "id": "rust",
  "name": "Rust 系统编程",
  "category": 1,
  "status": "locked",
  "desc": "内存安全的系统级编程语言",
  "prerequisites": ["lang_cpp"]
}
```

### 添加新分类示例

```json
{
  "id": 8,
  "name": "区块链技术",
  "color": "#ffd700"
}
```

## 🌈 主题定制

修改 `tailwind.config.js` 中的颜色配置：

```javascript
colors: {
  'cyber-blue': '#00f2ff',    // 主题蓝色
  'cyber-pink': '#ff00ff',    // 主题粉色
  'cyber-purple': '#9d00ff',  // 主题紫色
  'cyber-dark': '#0a0e27',    // 背景色
}
```
